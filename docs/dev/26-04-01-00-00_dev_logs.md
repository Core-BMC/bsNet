# Dev Log — 2026-04-01

> 이 로그는 `docs/2.5_log_experiment_20260401.md` 에서 재구성.

---

## Session 1 — Figure 1–7 스타일 완성 + ds000243 파이프라인 인프라 구축

### Changed
- **`src/visualization/plot_figure1_ds007535.py`**:
  - 모든 패널 (A, B, C) 6-atlas 표시. Schaefer200 opaque (α=0.92, lw=2.5), 나머지 faded (α=0.25, lw=1.0).
  - Panel A legend: "2 min" 레이블 제거, ρ̂T(bs-Net) 아래 Schaefer200 faded 항목 추가.
  - G1/G2 행렬: `short-short` corr → **cross-subject reliability** `R[i,j] = r over N subjects(fc_cond[:,i,j], fc_ref[:,i,j])`. Full BS-NET pipeline per-subject 적용 후 Fisher-z per-connection 보정.
  - G3 scatter: sub-01 단독 → **N=30 전체 피험자**, exemplar 강조 (s=120, 파란 테두리), 나머지 faded (α=0.22).
  - NPZ 캐싱: 첫 실행 계산 저장, 이후 즉시 로드.

- **`src/visualization/plot_figure2_validation.py`**: 전면 재작성. 데이터 소스: ds007535 N=30, 6 atlases, full BS-NET pipeline. 4-panel (scatter r_fc vs ρ̂T / KDE paired 분포 / violin Δ by atlas / bar mean Δ±SD). Panel D ylim 고정 [0, 0.2].

- **`src/visualization/plot_figure7_classification.py`**: 2×2 violin → **1×2 grouped bar chart** 재설계. `BAR_WIDTH=0.28`, `GROUP_GAP=0.35`. CC200 실선 / CC400 해칭(`/`). 오차막대 ±1 SD (10 seeds × 5 folds).

- **`src/visualization/style.py`**: `ATLAS_META` dict 추가 (6 atlases: color, linestyle, marker, label, lw). `_alpha_for()`, `_lw_for()`, `_ms_for()` helper로 투명도 계층 일원화. Figure 1–7 전체 import.

### Added
- **`src/scripts/preprocess_ds000243.py`**: 순수 resting-state 전처리 (36P confound regression + bandpass 0.01–0.1 Hz, task regression 없음). `discover_subjects()` — `*desc-preproc_bold.nii.gz` flex glob.
- **`src/scripts/run_ds000243_batch.sh`**: 6-atlas × 10 seeds 배치 실행, skip-existing + 컬러 로깅.
- **`data/ds000243/{raw,timeseries_cache,results}/`**: 디렉토리 구조 스캐폴딩.
- **`run_duration_sweep.py` — ds000243 지원**: `DURATIONS_DS000243=[30,60,90,120,180,240,300,450]`, `TR_DS000243=2.0`, `ATLAS_CHOICES["ds000243"]`, `discover_subjects_ds000243()`, CLI `--dataset ds000243`.
- **`docs/INDEX.md`**: Figure 테이블 신규 파일명 + 스크립트 참조 업데이트, 2.5 로그 항목 추가.

### 핵심 설계 결정
- **cross-subject reliability 벡터화**: `_corr_vectorized(A, B)` — [N × r²] → [r²] Python loop 없음.
- **Fisher-z per-connection 보정**: `z_shift = arctanh(ρ̂T) − arctanh(r_fc)` → `fc_bsnet[i,j] = tanh(arctanh(fc_bootstrap[i,j]) + z_shift)`.
- **투명도 계층 일원화**: `is_main = atlas == "schaefer200"` 단일 플래그로 전 패널 alpha/lw/ms/zorder 제어.

### TODO
- [ ] ds007535 bold.nii.gz 다운로드 완료 확인 → `preprocess_ds007535.py` 실행
- [ ] `run_duration_sweep.py --dataset ds007535 --n-seeds 10 --n-jobs 4` 실행
- [ ] ds000243 fMRIPrep 출력물 업로드 완료 후 → `preprocess_ds000243.py` 실행
- [ ] Figure 1 최종 플로팅 (실데이터 sweep 결과 기반)
- [ ] style.py에 Gray/Amber/Blue 3색 스키마 공식 상수 등록
- [ ] Figure 2 검증 (ds000243 N=50 기반으로 교체 예정)
