# Dev Log — 2026-04-17

## Session 1 — 13:48 (Fig 3 ABIDE 메인/보충 분리)

### Added
- **`src/visualization/plot_figure_s3_abide_filtered_consort.py`**: 기존 Fig 3(Strict filtered + CONSORT) 로직을 Supplementary 전용 스크립트로 분리.
  - 출력 파일명: `FigS3_ABIDE_Filtered_CONSORT.png`
  - 기본 동작: strict filtered 결과 사용, `--no-filter`는 legacy sensitivity 용도로 유지.

### Changed
- **`src/visualization/plot_figure3_abide.py`**: Main Fig 3를 N=468 대규모 검증 패널 전용으로 재정의.
  - CONSORT row 제거 (중복 메시지 해소)
  - 1×4 layout 고정: (A) scatter, (B) raw vs BS-NET distribution, (C) improvement Δ, (D) seed stability
  - 기본 입력을 unfiltered CSV(`abide_multiseed_{atlas}_10seeds.csv`)로 변경
  - `--filtered-strict` 옵션은 보조 확인용으로만 유지
- **`docs/3.1_res_figure_legends.md`**:
  - Fig 3 설명을 메인 N=468 validation 중심으로 정리
  - strict subset + CONSORT는 Supplementary S3로 분리 명시
- **`docs/3.2_res_abide_figure_legends.md`**:
  - S1/S2를 현재 구현(Progressive 6-level, k-stratification) 기준으로 정렬
  - S3를 strict filtered + CONSORT로 신규 정의
  - ceiling-method 비교는 S4/S5 후보로 재배치

### Fixed
- Main Fig 3와 Supplementary FigS1/S2 간 역할 중복(필터링 정당화 vs 최종 성능 보고) 해소.
- 문서-코드 불일치(메인에서 CONSORT 반복, S1/S2 정의 mismatch) 수정.

### Experiments
- 실행 없음 (Script Execution Policy 준수: 사용자가 직접 실행).
- 코드 정합성 확인만 수행:
  - `python3 -m py_compile src/visualization/plot_figure3_abide.py src/visualization/plot_figure_s3_abide_filtered_consort.py`

### TODO
- [ ] 사용자 로컬에서 Fig 3 / FigS3 렌더링 실행 후 레이아웃 최종 확인
- [ ] Fig 3 본문 수치(평균±SD, improved %)를 재생성 결과 기준으로 legend에 고정
- [ ] 필요 시 docs/INDEX.md Figure/Supplementary 매핑을 최신 체계(1–6 + S1+)로 전면 동기화
- [ ] AGENTS.md ↔ CLAUDE.md Current Status/Next TODO 완전 동기화 (session 번호 포함)

## Session 2 — 14:02 (Fig3/FigS3 분포 패널 스타일 통일)

### Changed
- **`src/visualization/plot_figure3_abide.py`**:
  - Panel B/C/D를 floating-bar 방식에서 **violin + boxplot + mean±SD(red diamond) + outlier dots**로 변경.
  - 디자인 기준: Fig 2 Panel D scheme.
- **`src/visualization/plot_figure_s3_abide_filtered_consort.py`**:
  - Panel C/D/E를 동일 스킴(violin+box+SD+outlier dots)으로 변경.

### Fixed
- Fig 2 Panel D와 Fig 3/FigS3 간 분포 패널 디자인 불일치 해소.

### Experiments
- 실행 없음 (Script Execution Policy 준수: 사용자가 직접 실행).
- 코드 정합성 확인:
  - `python3 -m py_compile src/visualization/plot_figure3_abide.py src/visualization/plot_figure_s3_abide_filtered_consort.py`

### TODO
- [ ] 사용자 로컬에서 Fig3/FigS3 재렌더링 후 violin width, outlier 밀도, SD diamond 가독성 최종 점검

## Session 3 — 14:18 (Fig 3 완료 확정, Fig 4 전환)

### Changed
- **Figure 3 완료 처리**:
  - Main Fig 3를 N=468 검증 패널 전용으로 확정 (CONSORT 분리 유지)
  - 분포 패널 스타일을 최종 확정: violin + boxplot + outlier dots (red diamond ±SD 제거)
  - violin width를 기본값(0.5)으로 확정

### Fixed
- Fig 3/FigS3 분포 패널에서 중복 에러바(whisker + mean±SD) 시각 혼선을 제거.

### TODO
- [ ] Figure 4(ADHD) 스크립트/캡션/출력물 점검 후 Fig 3와 스타일 일관성 확정
- [ ] 필요 시 Fig 4 분포 패널도 Fig 3와 동일 스킴(violin+box+outlier)으로 통일

## Session 4 — 14:25 (Fig 4 스타일 정렬 시작)

### Changed
- **`src/visualization/plot_figure4_adhd.py`**:
  - Panel C/D/E를 기존 floating-bar 스킴에서 **violin + boxplot + outlier dots**로 변경.
  - red diamond(±SD) 제거, Fig 3 최종 스킴과 일치하도록 통일.
- **`docs/3.1_res_figure_legends.md`**:
  - Fig 4 설명을 ADHD-200 PCP **N=399** 기준으로 갱신.
  - 패널 구성(A–E)과 현재 스크립트 구조에 맞춰 정렬.

### TODO
- [ ] 사용자 로컬에서 Fig 4 재렌더링 후 분포 패널 밀도/가독성 점검
- [ ] 필요 시 Fig 4 CONSORT 가시성(텍스트 크기/spacing) 미세 조정

## Session 5 — 14:46 (Patient utility 확장: reliability-aware clustering 파이프라인 추가)

### Added
- **`docs/5.12_patient_utility_reliability_aware_clustering.md`**:
  - 환자 데이터 활용성 보강 방향 문서 신규 작성.
  - `ρ̂T` strata(T1/T2/T3) 기반 HC/Patients 비지도 분리도 분석 설계 고정.
  - nilearn FC method 비교(correlation / partial / tangent) + KMeans 중심 평가 지표(ARI/NMI/BalAcc) 명시.
- **`src/scripts/run_reliability_aware_clustering.py`**:
  - ADHD-200 PCP strict subset에서 reliability-aware clustering 실행 스크립트 신규 추가.
  - 알고리즘: KMeans(주력), GMM, Spectral.
  - 출력: `adhd200_reliability_clustering_runs.csv`, `adhd200_reliability_clustering_summary.csv`.
- **`src/visualization/plot_patient_utility_clustering.py`**:
  - 분석 결과 시각화 스크립트 신규 추가.
  - 출력: `docs/figure/FigS6_Reliability_Aware_Clustering.png`.

### Changed
- **`docs/INDEX.md`**:
  - 5.12 문서 항목 추가.
- **`src/scripts/README.md`**:
  - 신규 분석 스크립트 및 시각화 스크립트 인덱스 반영.

### Experiments
- 실행 없음 (Script Execution Policy 준수: 사용자 직접 실행).

### TODO
- [ ] 사용자 로컬에서 clustering 스크립트 실행 후 strata별 ARI/NMI/BalAcc dose-response 확인
- [ ] FigS6 렌더링 확인 후 panel 라벨/축 범위 미세조정
- [ ] 결과가 안정적이면 Main Figure 후보(Fig 5/6 재배치)로 승격 여부 결정

## Session 6 — 15:16 (유의성 보강: permutation + class-balance correction)

### Changed
- **`src/scripts/run_reliability_aware_clustering.py`**:
  - `--balance-classes/--no-balance-classes` 옵션 추가 (기본: on)
    - repeat마다 다수 클래스 random downsampling으로 class-balance 보정
  - `--n-permutations` 옵션 추가 (기본: 200)
  - `--permute-primary-only/--no-permute-primary-only` 옵션 추가 (기본: on)
    - 기본은 KMeans 결과에만 permutation 적용
  - run-level 출력에 `n_subjects_analysis`, `n_adhd_analysis`, `n_control_analysis`, permutation p-value(ARI/BalAcc) 추가
  - summary 출력에 `ari_p_perm_mean`, `ari_sig_rate_p05`, `bal_acc_p_perm_mean`, `bal_acc_sig_rate_p05` 추가

### Fixed
- strata class imbalance(예: T3 ADHD/Control 불균형)에 따른 분리도 왜곡 가능성 완화.
- 성능 지표의 우연 일치 가능성을 permutation p-value로 정량화.

### TODO
- [ ] 사용자 로컬 재실행 후 T1/T2/T3의 p-value 패턴 확인
- [ ] 필요 시 n-permutations를 1000으로 올려 최종 수치 고정

## Session 7 — 15:24 (병렬 worker 지원 + BLAS 주의 반영)

### Changed
- **`src/scripts/run_reliability_aware_clustering.py`**:
  - `--n-jobs` 옵션 추가 (repeat-level 병렬화)
  - `ProcessPoolExecutor` 기반 반복 실행 병렬 경로 구현
  - worker 함수 `_run_one_repeat()` 분리
  - 기존 class-balance/permutation 로직이 병렬 경로에서도 동일 동작

### Note
- `ProcessPoolExecutor` + NumPy 조합이므로 BLAS thread 고정 환경변수 설정이 필수.

### TODO
- [ ] 사용자 로컬에서 `--n-jobs` 스케일링(예: 4/8/12) 벤치 후 적정값 고정

## Session 8 — 15:41 (FigS6 해석 안전장치 추가)

### Changed
- **`src/visualization/plot_patient_utility_clustering.py`**:
  - permutation p-value 컬럼 로드(`ari_p_perm_mean`, `bal_acc_p_perm_mean`) 추가
  - Panel A/B에 stratum별 best-method p-value annotation 추가
  - figure 중앙 워터마크 추가:
    - `EXPLORATORY (No robust significant separation)`
  - 하단 캡션(guardrail) 추가:
    - permutation 결과 기반으로 강한 HC/Patients 분리 주장 금지 문구 명시

### Fixed
- exploratory 결과를 본문에서 과대해석할 위험 완화.

### TODO
- [ ] FigS6 최종 렌더링 확인 (annotation 겹침 여부, 워터마크 alpha 미세조정)

## Session 9 — 16:20 (ADHD discrimination 인용 가이드 문서화)

### Added
- **`docs/5.13_adhd_discrimination_citation_guide.md`**:
  - ADHD vs HC discrimination 분석을 위한 **인용 중심 작성 가이드** 신규 작성.
  - manuscript에 바로 삽입 가능한 Methods/Results/Discussion 템플릿 문구 추가.
  - claim-to-citation 매핑(benchmark, site/confound, harmonization, generalization caveat, dataset citation) 정리.
  - BS-NET 통합 방식(ρ̂T 가중치, permutation/LOSO 권장) 명시.

### Changed
- **`docs/INDEX.md`**:
  - 5.13 가이드 문서 항목 추가.

### Experiments
- 실행 없음 (Script Execution Policy 준수: 사용자 직접 실행).

### TODO
- [ ] `run_reliability_aware_classification.py` 신규 구현 여부 결정 (tangent + linear model + LOSO + permutation).
- [ ] Figure 4/6 본문 서술에 5.13의 문구(과대주장 방지 문장 포함) 반영.

## Session 10 — 16:38 (reliability-aware supervised classification 구현)

### Added
- **`src/scripts/run_reliability_aware_classification.py`**:
  - ADHD-200 PCP strict subset 기반 ADHD vs Control 분류 스크립트 신규 추가.
  - 평가 스킴: `--eval-scheme {loso,stratified_kfold}` (기본 LOSO)
  - FC: `correlation`, `partial correlation`, `tangent`
  - 모델: `logistic_l2`, `linear_svm`
  - 옵션: class-balance downsampling, age/sex/site covariate, `rho_hat_T` sample-weight(`--rho-weight-gamma`), permutation p-value, repeat 병렬(`--n-jobs`)
  - 출력:
    - `data/adhd/pcp/results/adhd200_reliability_classification_runs.csv`
    - `data/adhd/pcp/results/adhd200_reliability_classification_summary.csv`

### Changed
- **`src/scripts/README.md`**:
  - 신규 classification 스크립트 항목 및 CLI 예시 추가.
- **`docs/5.13_adhd_discrimination_citation_guide.md`**:
  - classification 스크립트 상태를 “구현 완료”로 갱신.

### Experiments
- 실행 없음 (Script Execution Policy 준수: 사용자가 직접 실행).

### TODO
- [ ] 사용자 로컬에서 LOSO + primary(tangent/logistic) 설정으로 실행 후 p-value 안정성 확인
- [ ] 필요 시 FigS6와 별도로 supervised utility figure (예: FigS7) 추가 여부 결정
